# This file ensures the data/ directory is tracked by git.
# The SQLite database files (*.db, *.db-shm, *.db-wal) are gitignored.
# Run `python data/seed.py` after starting the backend to populate sample data.
